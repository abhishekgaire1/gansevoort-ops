import { RecordWasteView } from "../../_components/RecordWasteView";

/** "+ Record Waste" from the hub -- location -> item -> quantity/reason
 * -> review -> record, all in one place (Part 5). */
export default function NewInventoryWastePage() {
  return <RecordWasteView />;
}
